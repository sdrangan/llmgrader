// ----- Shared UI Dropdown Helpers -----

function openUnitDropdown() {
    const el = document.getElementById("unit-select");
    if (el) {
        el.focus();
        el.click();
    }
}

function openQuestionDropdown() {
    const el = document.getElementById("question-number");
    if (el) {
        el.focus();
        el.click();
    }
}

function openPartDropdown() {
    const el = document.getElementById("part-select");
    if (el) {
        el.focus();
        el.click();
    }
}

function openModelDropdown() {
    const el = document.getElementById("model-select");
    if (el) {
        el.focus();
        el.click();
    }
}